import { Field, inputClass, submitClass } from "@/components/admin/ui";
import type { Client } from "@/lib/crud/types";

export function ClientForm({ client, action }: { client?: Client; action: (formData: FormData) => Promise<void> }) {
  return <form action={action} className="mt-8 grid gap-5 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><Field label="Nombre comercial / Cliente"><input className={inputClass} defaultValue={client?.name ?? ""} name="name" required /></Field><Field label="Razón social"><input className={inputClass} defaultValue={client?.business_name ?? ""} name="business_name" /></Field><Field label="Contacto"><input className={inputClass} defaultValue={client?.contact_name ?? ""} name="contact_name" /></Field><div className="grid gap-5 md:grid-cols-2"><Field label="Email"><input className={inputClass} defaultValue={client?.email ?? ""} name="email" type="email" /></Field><Field label="Teléfono"><input className={inputClass} defaultValue={client?.contact_phone ?? ""} name="contact_phone" /></Field></div><Field label="Dirección"><textarea className={`${inputClass} min-h-20`} defaultValue={client?.address ?? ""} name="address" /></Field><Field label="Notas"><textarea className={`${inputClass} min-h-24`} defaultValue={client?.notes ?? ""} name="notes" /></Field><Field label="Estado"><select className={inputClass} defaultValue={client?.status ?? "active"} name="status"><option value="active">Activo</option><option value="inactive">Inactivo</option></select></Field><button className={submitClass} type="submit">Guardar cliente</button></form>;
}
