import { Field, inputClass, submitClass } from "@/components/admin/ui";
import type { Client, Trap } from "@/lib/crud/types";

export function TrapForm({ trap, clients, action }: { trap?: Trap; clients: Client[]; action: (formData: FormData) => Promise<void> }) {
  return <form action={action} className="mt-8 grid gap-5 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><Field label="Cliente"><select className={inputClass} defaultValue={trap?.client_id ?? ""} name="client_id" required><option value="">Seleccionar cliente</option>{clients.map((client) => <option key={client.id} value={client.id}>{client.name}</option>)}</select></Field><Field label="Etiqueta"><input className={inputClass} defaultValue={trap?.label ?? ""} name="label" required /></Field><div className="grid gap-5 md:grid-cols-2"><Field label="Sitio"><input className={inputClass} defaultValue={trap?.site ?? ""} name="site" required /></Field><Field label="Zona"><input className={inputClass} defaultValue={trap?.zone ?? ""} name="zone" required /></Field></div><Field label="Código público"><input className={inputClass} defaultValue={trap?.public_code ?? ""} name="public_code" placeholder="Se genera automáticamente si se deja vacío" /></Field><Field label="Ubicación detallada"><textarea className={`${inputClass} min-h-24`} defaultValue={trap?.location_description ?? ""} name="location_description" /></Field><Field label="Estado"><select className={inputClass} defaultValue={trap?.status ?? "active"} name="status"><option value="active">Activa</option><option value="inactive">Inactiva</option><option value="maintenance">Mantenimiento</option><option value="lost">Perdida</option></select></Field><button className={submitClass} type="submit">Guardar trampa</button></form>;
}
