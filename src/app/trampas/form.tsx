import { Field, inputClass, submitClass } from "@/components/admin/ui";
import type { Client, Site, Trap, Zone } from "@/lib/crud/types";

export function TrapForm({ trap, clients, sites = [], zones = [], action }: { trap?: Trap; clients: Client[]; sites?: Site[]; zones?: Zone[]; action: (formData: FormData) => Promise<void> }) {
  return <form action={action} className="mt-8 grid gap-5 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><Field label="Cliente"><select className={inputClass} defaultValue={trap?.client_id ?? ""} name="client_id" required><option value="">Seleccionar cliente</option>{clients.map((client) => <option key={client.id} value={client.id}>{client.name}</option>)}</select></Field><Field label="Nombre de trampa"><input className={inputClass} defaultValue={trap?.name ?? ""} name="name" required /></Field><div className="grid gap-5 md:grid-cols-2"><Field label="Sitio existente"><select className={inputClass} defaultValue={trap?.site_id ?? ""} name="site_id"><option value="">Crear/usar sitio principal</option>{sites.map((site) => <option key={site.id} value={site.id}>{site.name}</option>)}</select></Field><Field label="Zona existente"><select className={inputClass} defaultValue={trap?.zone_id ?? ""} name="zone_id"><option value="">Crear/usar zona general</option>{zones.map((zone) => <option key={zone.id} value={zone.id}>{zone.name}</option>)}</select></Field></div><div className="grid gap-5 md:grid-cols-2"><Field label="Nuevo sitio (si no eliges uno)"><input className={inputClass} name="site_name" placeholder="Sitio principal" /></Field><Field label="Nueva zona (si no eliges una)"><input className={inputClass} name="zone_name" placeholder="Zona general" /></Field></div><Field label="Código público"><input className={inputClass} defaultValue={trap?.public_code ?? ""} name="public_code" placeholder="TG-XXXXXX si se deja vacío" /></Field><Field label="Ubicación esperada"><textarea className={`${inputClass} min-h-24`} defaultValue={trap?.expected_location ?? ""} name="expected_location" /></Field><Field label="Código NFC"><input className={inputClass} defaultValue={trap?.nfc_code ?? ""} name="nfc_code" /></Field><Field label="Estado"><select className={inputClass} defaultValue={trap?.status ?? "active"} name="status"><option value="active">Activa</option><option value="inactive">Inactiva</option><option value="maintenance">Mantenimiento</option><option value="missing">No encontrada</option></select></Field><button className={submitClass} type="submit">Guardar trampa</button></form>;
}
