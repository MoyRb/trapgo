import { notFound } from "next/navigation";
import { AdminShell, ButtonLink, ErrorState } from "@/components/admin/ui";
import { createSupabaseServerClient } from "@/lib/supabase/server";
import { updateTrap } from "../../actions";
import { TrapForm } from "../../form";
export default async function EditTrapPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const supabase = await createSupabaseServerClient(); const [trapResult, clientsResult, sitesResult, zonesResult] = await Promise.all([supabase.from("traps").select("*").eq("id", id).single(), supabase.from("clients").select("*").order("name"), supabase.from("sites").select("*").order("name"), supabase.from("zones").select("*").order("name")]); if (!trapResult.data && !trapResult.error) notFound(); const error = trapResult.error ?? clientsResult.error ?? sitesResult.error ?? zonesResult.error; if (error) return <AdminShell title="Editar trampa"><ErrorState message={error.message} /></AdminShell>; return <AdminShell title="Editar trampa" actions={<ButtonLink href={`/trampas/${id}`} variant="secondary">Cancelar</ButtonLink>}><TrapForm trap={trapResult.data} clients={clientsResult.data ?? []} sites={sitesResult.data ?? []} zones={zonesResult.data ?? []} action={updateTrap.bind(null, id)} /></AdminShell>; }
